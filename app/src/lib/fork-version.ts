/**
 * Version of this multi-repository fork of GitHub Desktop.
 *
 * This is independent from the upstream application version found in
 * `app/package.json` (which tracks the GitHub Desktop release this fork is
 * based on). Bump this when shipping fork features and keep `VERSION.md` (at
 * the repository root) in sync — it holds the human-readable changelog for
 * each fork version.
 */
export const ForkVersion = '0.3.0'

/** Short product label for the fork, shown alongside the version. */
export const ForkName = 'Multi-Repo'
