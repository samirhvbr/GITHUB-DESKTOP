# GITHUB_DESKTOP — Estado e Pendências

> Snapshot datado **23/06/2026** (atualizado ao **fim do dia**). A fonte viva de
> continuidade é `estado-atual.md`; o plano por fase é `plano-tecnico.md`. Este
> arquivo é a foto consolidada do dia.

## O que é

Fork pessoal do **GitHub Desktop** (`git@github.com:samirhvbr/GITHUB_DESKTOP.git`) — app
**Electron + TypeScript + React** (yarn / Node 24.15.0). Objetivo do fork: torná-lo um
gerenciador **multi-repositório**. Demandas em `SAMIR-PROJETO.md`.

## Feito em 23/06/2026

- Pasta `.continue/` criada, commitada e **pushada** (sobrevive a re-clones).
- **Investigação técnica** do código (4 eixos) → `plano-tecnico.md`, mapeando cada demanda
  aos pontos de extensão. Descoberta-chave: a infra de status multi-repo
  (`localRepositoryStateLookup` + `RepositoryIndicatorUpdater`) já existe.
- **Fase 1 — Painel multi-repo** implementada, validada (`tsc` + `eslint`) e pushada:
  - `app/src/ui/multi-repo-dashboard/multi-repo-dashboard.tsx` (novo) — lista todos os
    repos, destacando os com demanda (commit pendente / atrás do remoto); clique abre.
  - Estado `showMultiRepoDashboard` + AppStore/Dispatcher; render em `app.tsx`; ponto de
    entrada no foldout da lista de repos; estilos em `_multi-repo-dashboard.scss`.

## Estado do git

- Branch: `multi-repo-dashboard` (em dia com `origin`).
- Commits do dia: `2b2a5fdd93` (notas/config), `1925b6232b` (docs/plano), `c6b039d843` (Fase 1).
- Remote único `origin` (sem upstream `desktop/desktop`).

## Pendências

### Imediata
- **Validação visual da Fase 1**: rodar `yarn start` (no Windows) e conferir o dashboard.
  No Linux local só dá p/ validar tipos/lint (Node 20 < 24 exigido) — instalar com
  `node vendor/yarn-1.21.1.js install --ignore-engines`.

### Próximas fases (`plano-tecnico.md`)
- **Fase 2 — Clone seletivo (demandas 3-5):** multi-seleção de repos; enterprise/corporativo
  + repos públicos por nome de usuário; pasta raiz comum ou individual. ← **em andamento**.
- **Fase 3 — Push agendado (demanda 6):** agendador de push por repo + commit "padrão" opt-in.

### Futura
- Login centralizador / controle remoto (modelo "sshvterm").
