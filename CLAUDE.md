# CLAUDE.md

Notes for Claude Code working in this repo. This is a **fork** of [GitHub Desktop](https://desktop.github.com) (`desktop/desktop`); the proprietary work lives on the `multi-repo-dashboard` branch. Keep upstream files clean to avoid merge conflicts when syncing.

## 🔄 Before you start: `git pull`

**ALWAYS** check for remote updates before writing or changing anything in this repo:

```bash
git pull          # already allow-listed
```

Working on a stale base causes conflicts. Pull first, every time. To inspect only: `git fetch && git status`.
